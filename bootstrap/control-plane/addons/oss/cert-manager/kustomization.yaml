resources:
  - repository.yaml
  - release.yaml
configMapGenerator:
  - name: cert-manager-values
    namespace: flux-system
    files:
      - values.yaml=../../../../../charts/addons/cert-manager/values.yaml
      - environment-values.yaml=../../../../../environments/{{metadata.labels.environment}}/addons/cert-manager/values.yaml
      - cluster-values.yaml=../../../../../clusters/{{metadata.labels.cluster_name}}/addons/cert-manager/values.yaml
configurations:
  - kustomizeconfig.yaml
